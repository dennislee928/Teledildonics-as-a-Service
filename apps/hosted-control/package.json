{
  "name": "@taas/hosted-control",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@taas/domain-sdk": "0.1.0",
    "@taas/embed-sdk": "0.1.0"
  },
  "scripts": {
    "build": "npm run typecheck && esbuild src/main.ts --bundle --format=esm --outfile=dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}

